<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/colorOnPrimary" />
    <stroke
        android:width="@dimen/border_width"
        android:color="@color/popup_border" />
    <padding
        android:left="@dimen/menu_padding"
        android:top="@dimen/menu_padding"
        android:right="@dimen/menu_padding"
        android:bottom="@dimen/menu_padding" />

    <corners android:radius="@dimen/corner_radius" />
</shape>